# `JemalWeb` keeps the controllers and views which
# define your web interface.
#
# `JemalWeb` is also responsible for housing the middleware
# and back-end logic related content.
module JemalWeb; end
